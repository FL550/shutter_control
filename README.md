To use this script, you have to create a `.env` file with the following content:

```
MQTT_SERVER = 
MQTT_USERNAME = 
MQTT_PASSWORD = 
```

You also have to probably adjust the script itself. For questions feel free to add an issue.
